<%- include("partials/header", { title: "Settings", subtitle: "Price / UPI / Delivery rules", key, active: "settings" }) %>

<div class="grid">
  <div class="card">
    <h2>Quick links</h2>
    <div class="chips">
      <span class="chip ok">Live Admin</span>
      <span class="chip">Use bottom tabs</span>
      <span class="chip warn">Keep key safe</span>
    </div>
  </div>

  <div class="card">
    <h2>Business Settings</h2>

    <form method="POST" action="/admin/settings?key=<%= key %>">
      <div class="row row-2">
        <div>
          <label>Product Name</label>
          <input name="product_name" value="<%= settings.product_name %>" />
        </div>
        <div>
          <label>Unit Label</label>
          <input name="unit_label" value="<%= settings.unit_label %>" />
        </div>
      </div>

      <div class="row row-2">
        <div>
          <label>Today Price (per unit)</label>
          <input name="today_price_per_unit" value="<%= settings.today_price_per_unit %>" />
        </div>
        <div>
          <label>Free Delivery Above</label>
          <input name="free_delivery_threshold" value="<%= settings.free_delivery_threshold %>" />
        </div>
      </div>

      <div class="row row-2">
        <div>
          <label>Delivery Charge Amount</label>
          <input name="delivery_charge_amount" value="<%= settings.delivery_charge_amount %>" />
        </div>
        <div>
          <label>UPI ID</label>
          <input name="upi_id" value="<%= settings.upi_id %>" />
        </div>
      </div>

      <button class="btn" type="submit">Save Settings</button>
    </form>
  </div>
</div>

<%- include("partials/footer", { key, active: "settings" }) %>
